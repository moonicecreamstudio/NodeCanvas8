using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ReactionDetection : MonoBehaviour
{
    public Transform[] allTargets;
    public Transform bestTarget;
    public float bestCriteriaValue;
    public float currentCriteriaValue;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
